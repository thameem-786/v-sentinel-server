{
  "name": "v-sentinel-server",
  "version": "1.0.0",
  "description": "V-Sentinel WebSocket Relay Server — ESP32 ↔ Browser",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
